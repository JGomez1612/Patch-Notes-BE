// Imports
import express from "express";

// Setup
const PORT = 3000;
const app = express();

// Middleware

// Routes

// Global Error Handling

// Listener
app.listen(PORT, () => {
    console.log(`Server Running on PORT: ${PORT}`);
});